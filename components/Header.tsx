import { BuyButton, LoginButton } from "./ui";

const Header = async () => {
  return (
    <div>
      <h1>Header</h1>
      <BuyButton />
      <LoginButton />
    </div>
  );
};

export default Header;
